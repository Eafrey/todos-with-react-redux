const userInfos = {
  avator: 'avator',
  name: 'chs',
  gender: 'male'
};

const headers = (state = userInfos, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default headers;
